# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on push nuxt alpine
'on':
  push:
    branches:
      - master
    paths:
      - 'nuxt/alpine/**'

defaults:
  run:
    working-directory: ./nuxt/alpine

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn && yarn generate
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_IGORJACAUNA_196EA }}'
          channelId: live
          projectId: igorjacauna-196ea
